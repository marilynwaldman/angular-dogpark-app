import { DogparkAppPage } from './app.po';

describe('dogpark-app App', () => {
  let page: DogparkAppPage;

  beforeEach(() => {
    page = new DogparkAppPage();
  });

  it('should display message saying app works', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('app works!');
  });
});
